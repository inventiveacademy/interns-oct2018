{
  "name": "web-services",
  "version": "1.0.0",
  "description": "server side of our trello kpi dashboard",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "cross-env aws s3 sync ../client s3://interns-oct2018 --delete --profile internship",
    "deploy": "cross-env AWS_PROFILE=internship sls deploy"
  },
  "author": "camilo thinh roslyn",
  "license": "MIT",
  "dependencies": {
    "cross-env": "^5.2.0",
    "serverless": "^1.32.0"
  },
  "devDependencies": {
    "serverless-offline": "^3.31.3"
  }
}
